# Must match the CRM's WEBHOOK_SECRET
WEBHOOK_SECRET=dev-shared-secret-change-me

# Worker pool / backpressure
WORKER_COUNT=4
QUEUE_MAXSIZE=10000

# Callback resilience
CALLBACK_MAX_RETRIES=5
CALLBACK_BACKOFF_BASE=0.5

# Lifecycle time compression (1.0 = a few seconds end-to-end)
SPEED=1.0
